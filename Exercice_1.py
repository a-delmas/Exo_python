# First_name = "principessa"

# print(len(First_name))

# for i in range (len(First_name)):
#     print((First_name[i]))

                                    # for i in range (len(First_name)):
                                    # First_name.replace ('m', 'l')
                                    # print(First_name)
# print(First_name)
# First_name = list(First_name)
# First_name[2] = 'e'
# First_name = "".join(First_name)
# print(First_name)

                                    # First_name.caractereMin = "m"
                                    # lower=maj upper=min
                                    # First_name.capitalize()
                                    # print First_name.capitalize()
# print(First_name.upper())
# print(First_name.lower())
# print(First_name.capitalize())
# print(First_name.title())

# print(len(First_name)[0])

# c = 'e'
# idx = -1

# for i in range (len(First_name)):
#     if First_name[i] == c:
#         idx = i

# print(idx)

# print(First_name.find('e'))

# print(e in First_name)


First_name = "anna"

"""
if First_name == ''.join(reversed(First_name)):
    print("Palindrome")
else:
    print("Pas palindrome")
"""
# if len(str First_name)[0] == len(str First_name)[i]:
#     if len(First_name)[1] == len(First_name)[i-1]:

result = True
for i in range (len(First_name)):
    if First_name[i] != First_name[len(First_name) - 1 - i]:
        result = False

print(result)

                        # def is_palindrome(First_name):
                        #     return First_name == First_name[::-1]

                        # mot = input("Votre mot: ")
                        # S = mot
                        # P = is_palindrome (First_name)
                        # if P == True:
                        #     print(" est palindrome")
                        # else:
                        #     print(" n'est pas palindrome et donne: ", S[::-1])